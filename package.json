{
  "scripts": {
    "dev": "tailwindcss -i main.css -o dist/output.css --minify --watch",
    "build": "tailwindcss -i main.css -o dist/output.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.7"
  }
}
