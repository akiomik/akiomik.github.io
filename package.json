{
  "scripts": {
    "dev": "tailwindcss -i main.css -o dist/output.css --watch",
    "build": "tailwindcss -i main.css -o dist/output.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.7"
  }
}
