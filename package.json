{
  "scripts": {
    "dev": "tailwindcss -i main.css -o dist/main-4a6a.min.css --minify --watch",
    "build": "tailwindcss -i main.css -o dist/main-4a6a.min.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.7"
  }
}
